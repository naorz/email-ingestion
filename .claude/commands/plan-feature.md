---
description: Plan a new feature with a step-by-step development guide
---

Load and execute the workflow defined in `{project-root}/.agents/workflows/new-feature.md`.

The user wants to plan the following feature: $ARGUMENTS

Follow every phase in the workflow document. Create the planning document under `docs/planning-features/`. Adhere to all project skills defined in `.agents/skills/`.
